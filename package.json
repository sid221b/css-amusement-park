{
  "name": "css-park",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "sass --watch scss:css",
    "copy:files": "copyfiles index.html docs/ && copyfiles css/styles.css docs/",
    "build": "sass scss:css && npm run copy:files"
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "sass": "^1.35.1"
  }
}
